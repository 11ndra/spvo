# PROJECT HANDOFF — IDPS Course

Последнее обновление: 2026-09-18  
Текущий стабильный релиз: `v2.27.1`  
Текущий release candidate с актуализированным handoff: `v2.29-rc4`

Этот файл предназначен для продолжения разработки проекта в новом диалоге. Он не должен добавляться в навигацию MkDocs/GitHub Pages.

---

## 1. Что это за проект

Университетский курс по дисциплине **«Системы обнаружения и предотвращения вторжений (IDPS)»** для студентов специальности **«Системы информационной безопасности»**.

Курс не является подготовкой под конкретную должность (`SOC Analyst`, `Security Engineer`, `Architect` и т. п.) и не должен строиться как ролевой буткемп.

Основная цель курса:

> сформировать у студента целостное инженерное представление о системах обнаружения и предотвращения вторжений: источниках данных, наблюдаемости, принципах анализа, методах обнаружения, архитектуре размещения, ограничениях, предотвращении, оценке эффективности и эксплуатации.

Suricata, Linux Audit, Zeek, Wazuh и другие продукты используются как реализации отдельных принципов, а не как предмет курса сами по себе.

---

## 2. Зафиксированная дидактическая структура

Порядок Глав 1–8 считается **замороженным**, если не найдено реальное техническое противоречие.

1. **Что такое IDS/IPS и зачем они нужны**
2. **Какие существуют виды IDS/IPS**
3. **Из чего состоит IDS/IPS и как она работает**
4. **Где и как размещают IDS/IPS**
5. **Как IDS/IPS обнаруживает подозрительную активность**
6. **Что такое правила и как они устроены**
7. **Почему IDS/IPS ошибается и чего она не видит**
8. **Как проверить и обосновать эффективность IDS/IPS**

Дидактический принцип:

```text
сначала элементарное понимание
        ↓
затем устройство и наблюдаемость
        ↓
затем методы обнаружения
        ↓
затем правила
        ↓
затем ограничения и ошибки
        ↓
затем строгая оценка эффективности
```

Нельзя начинать курс с Detection Engineering, TP/FP/FN, parser ambiguity, TCP reassembly или сложной онтологии — студент сначала должен понимать, что такое IDS/IPS, зачем они нужны и что именно они наблюдают.

---

## 2.1. Визуальная архитектура с v2.29-rc3 / reference pass v2.29-rc4

В проекте уже существовали `docs/assets/stylesheets/extra.css` и `docs/assets/javascripts/course.js`. Начиная с `v2.29-rc3`, новые визуальные компоненты не добавляются хаотично в эти файлы: введены отдельные `idps-system.css` и `idps-system.js`.

Production-подход:

- Mermaid — простые/часто меняющиеся схемы;
- HTML + CSS — карточки, сравнения, небольшие responsive-модели;
- SVG + CSS — сложные технические схемы с точной геометрией;
- JavaScript — только meaningful progressive enhancement;
- PNG/WebP — реальные скриншоты и фактические результаты интерфейсов.

Генеративные изображения Canva/Gemini не являются production source of truth для технических схем. Семантика задаётся вручную в HTML/Mermaid/SVG.

В `v2.29-rc4` Глава 1 полностью пройдена сверху вниз и стала reference implementation новой design system. В ней переиспользуются `idps-figure`, `idps-policy-*`, `idps-equation`, `idps-grid/idps-card`, `idps-switcher`, `idps-process`, `idps-result-card`, `idps-evidence-*`, `idps-contrast`, `idps-decision`, `idps-stack` и `idps-summary-*`. Решения по каждому элементу записаны в `internal/resources/chapter1-visual-audit.md`.

Следующий визуальный шаг — пройти Главу 2 тем же методом, не копируя компоненты механически: semantic modifier используется только если роль элемента действительно совпадает.

---

## 3. Текущее состояние теоретической части

### Актуальная новая редакция

- Глава 1 — готова в новой структуре.
- Глава 2 — готова в новой структуре.
- Глава 3 — готова в новой структуре.
- Глава 4 — готова в новой структуре.
- Глава 5 — готова в новой структуре.
- Глава 6 — готова в новой структуре.

Файлы:

```text
docs/course/01-intro.md
docs/course/02-classification.md
docs/course/03-detection.md
docs/course/04-detection-methods.md
docs/course/05-firewall-vs-idps.md
docs/course/06-rules.md
```

Файлы Глав 4–5 сохраняют legacy names ради стабильности существующих ссылок; содержание и `mkdocs.yml` являются источником истины для текущих названий.

### Пока предыдущая редакция

Главы 7–8 **не считать окончательно готовыми**. Они оставлены только как рабочий материал предыдущей версии и должны быть последовательно заменены.

```text
legacy/course/07-placement.md
legacy/course/08-detection-engineering.md
```

Старая `legacy/course/06-detection-quality.md` также сохранена только как архив предыдущей структуры и полностью заменена новой `docs/course/06-rules.md`. Legacy-материалы и старая предварительная проверка знаний находятся вне `docs/`, не публикуются через MkDocs/GitHub Pages и не должны попадать в студенческий поиск.

### Consistency correction v2.27.1

Перед переходом к Главе 6 закрыт consistency debt v2.27: исправлены две относительные ссылки Главы 5, переход в конце Главы 3, статус legacy Pre-Lab в Roadmap, единая модель QA-статусов ЛР №1–4 и фиктивные ссылки на отсутствующие SVG в визуальных служебных материалах. Содержательная последовательность курса не изменена.

### Classroom readiness candidate v2.28-rc3

Перед сегодняшними лабораторными устранён student-facing и практический долг:

- курс на главной начинается с Главы 1, а не с установки инструмента;
- добавлен обязательный неоценочный `Практикум 0 — Знакомство с рабочей IDPS`;
- ЛР №1 больше не является первым знакомством с Suricata и называется «Первое сетевое обнаружение»;
- developer-only QA statuses удалены со студенческих страниц и сохраняются только во внутренних материалах;
- маршрут курса заменён на однозначную вертикальную последовательность;
- legacy chapters/Pre-Lab и служебные audit/evidence материалы вынесены из `docs/`;
- в публикуемых downloads оставлены только актуальные classroom bundles;
- Lab01/Lab02 обновлены до `v0.5`: учебный HTTP-сервис привязан только к `10.13.37.20:8080`;
- environment setup обновлён до `v1.1`: readiness check требует Suricata 8.x;
- добавлен обязательный `QA_RELEASE_GATE.md`, определяющий достаточный аудит курса;
- добавлен `TEACHER_CLASSROOM_CHECKLIST.md` для быстрого go/no-go перед занятием;
- GitHub Pages workflow закреплён на Python 3.13, MkDocs 1.6.1 и Material 9.7.7 и после сборки проверяет ссылки в `site/`;
- добавлен `.gitattributes`: каноническое окончание строк репозитория — `LF`, бинарные артефакты исключены из преобразования;
- source/static gate теперь отклоняет `CRLF` и lone `CR` в контролируемых текстовых файлах;
- удалён конфликт двух поколений CSS для `.course-route`: маршрут имеет один однозначный вертикальный layout на desktop и mobile;
- зафиксирована причина неудачного deploy `v2.28-rc1`: release ZIP был наложен на более старую Git working tree, поэтому удалённые из RC файлы физически остались под `docs/`; consistency checker корректно остановил публикацию;
- добавлен `tools/migrate_v228.py` и upgrade-процедура для GitHub Desktop, чтобы stale public files и устаревшие bundles удалялись до commit/push.
- исправлена ошибка `tools/check_site_links.py`, остановившая deploy rc2: checker теперь учитывает project base path `/spvo/` из `site_url` и корректно отображает `/spvo/...` на содержимое `site/`; чужие root-relative пути остаются ошибкой.

Восьмиглавая теоретическая последовательность не изменена.

### Focused classroom correction v2.28-rc4

Поверх успешно задеплоенного `v2.28-rc3` внесён минимальный classroom patch без изменения структуры курса и CI:

- исправлены Mermaid-схемы, которые на GitHub Pages отображались как raw `flowchart` или `Syntax error in text`;
- вопросы устной защиты ЛР №1–4 вынесены из student-facing страниц в `TEACHER_LAB_CHEATSHEET.md`;
- Практикум 0 сохраняет только понятные студенту операционные learning outcomes;
- актуальные classroom bundles повторно проверены на целостность архивов, LF/CRLF и shell/Python syntax; эти проверки не заменяют end-to-end runtime на Ubuntu/VirtualBox.

### Глава 6 — завершена в новой редакции

**Глава 6 — «Что такое правила и как они устроены»** добавлена как `docs/course/06-rules.md`.

Она продолжает Главу 5 и фиксирует разграничения:

- метод обнаружения ≠ синтаксис правила;
- область применения правила ≠ совпадение;
- представление данных ≠ источник данных;
- `flow`/state context ≠ отдельная методология обнаружения;
- `msg`/`sid`/`rev`/классификация ≠ условие совпадения;
- правило ≠ alert ≠ incident;
- threshold/suppression ≠ исправление detection condition;
- action `drop` ≠ гарантированное предотвращение вне подходящего IPS/inline-режима.

Suricata используется как знакомая практическая реализация. Сквозной пример — уже использованное в ЛР №1 учебное правило `SID 1000001`; новая продуктовая подсистема ради главы не вводится.

## 4. Текущая лабораторная лестница

### Шаг 00 — Подготовка лабораторной среды

Студент самостоятельно разворачивает две VM в VirtualBox:

```text
Ubuntu Desktop 24.04.x
idps-client
10.13.37.10/24

Ubuntu Server 24.04.x
idps-server
10.13.37.20/24
```

Сетевая модель:

```text
Adapter 1 = NAT
Adapter 2 = Internal Network "IDPS-LAB"
```

Статические адреса `10.13.37.10/24` и `10.13.37.20/24` задаются на учебном интерфейсе. Второй default gateway на `IDPS-LAB` не создаётся.

Актуальный пакет подготовки:

```text
idps-environment-setup-v1.1.tar.gz
idps-environment-setup-v1.1.zip
```

### Практикум 0 — Знакомство с рабочей IDPS

Неоценочный bridge между подготовкой среды и первым экспериментом. Студент находит версию Suricata, основной конфигурационный файл, состояние службы, интерфейс `IDPS-LAB`, место учебного правила и EVE JSON. Синтаксис правил не изучается.

### ЛР №1 — Первое сетевое обнаружение

Актуальный пакет: `idps-lab01-bundle-v0.5.zip`. Ключевой тезис: `ОПОВЕЩЕНИЕ ≠ КОМПРОМЕТАЦИЯ`. Готовое правило используется как предоставленное машинно-исполняемое условие; его синтаксис отложен до Главы 6.

### ЛР №2 — Один эпизод, два источника данных

Актуальный пакет: `idps-lab02-bundle-v0.5.zip`. Ключевой тезис: `ОДИН ЭПИЗОД → РАЗНЫЕ СЛЕДЫ`. Linux Audit используется как источник host telemetry, а не выдаётся за полноценную HIDS.

### ЛР №3 — Точка наблюдения

Актуальный пакет: `idps-lab03-bundle-v0.2.zip`. Ключевой тезис: точка наблюдения оценивается относительно конкретного потока. Два контролируемых пути позволяют сравнить `LAB_IFACE` и `NAT_IFACE`.

### ЛР №4 — Четыре основания решения

Актуальный пакет:

```text
idps-lab04-bundle-v0.2.zip
```

Перед созданием bundle зафиксирована таблица согласованности:

```text
design/lab04-alignment.md
```

Один чистый сценарий формирует 19 HTTP-событий в `lab4-events.jsonl`. Один и тот же журнал анализируется четырьмя предоставленными режимами детектора:

```text
точный маркер                 → сигнатурная методология
нарушение модели состояния     → анализ состояния/семантики
>=5 /catalog за 2 секунды      → фиксированное условие над серией
всплеск относительно профиля   → аномальное обнаружение
```

Ключевой тезис: **фиксированный порог над серией событий не равен аномальному обнаружению**. Термин `behavioral` не вводится как отдельная универсальная четвёртая методология. Студент не пишет собственные правила; это намеренно отложено до Главы 6.

### Следующий лабораторный шаг

Не создавать ЛР №5 до завершения Главы 6 и её consistency table. Наиболее естественная следующая практическая задача — выразить несколько уже понятных detection conditions в rule syntax и проверить positive/negative behavior, но точный scope должен следовать из окончательной Главы 6.

## 5. Жёсткое правило согласования теории и лабораторных

Каждая лабораторная должна непосредственно закреплять уже изученную теорию.

Нельзя вводить в ЛР понятие, которое студент ещё не проходил, как если бы оно было ему известно.

Перед выпуском каждой связки проверить:

```text
что объяснено в главе?
        ↓
что именно студент делает руками?
        ↓
какой наблюдаемый результат получает?
        ↓
какой вывод он имеет право сделать?
```

Если вывод лабораторной не следует из её артефактов — лаборатория считается логически некорректной.

---

## 6. Правила проектирования лабораторных

1. **Один эксперимент — одна понятная изменяемая переменная**, если только лаборатория специально не изучает взаимодействие нескольких факторов.
2. Перед IDS сначала подтверждается существование самого события/трафика, если это необходимо для вывода.
3. **Отсутствие alert не является доказательством отсутствия события.**
4. При необходимости использовать независимый источник подтверждения, но учитывать границы его доказательной силы.
5. Application log подтверждает обработку конкретного L7-запроса, но не является универсальным журналом L3/L4 активности.
6. `tcpdump` подтверждает наличие пакетов в конкретной точке захвата, а не «физическое прохождение по проводу» в общем случае.
7. Не смешивать старые и новые артефакты: один эксперимент должен иметь отдельный output directory или временную границу.
8. Все shell-переменные, необходимые в новом терминале, должны явно загружаться/определяться; нельзя рассчитывать на скрытое состояние предыдущего shell.
9. Pre-flight проверяет готовность зависимостей, но **не заменяет end-to-end runtime QA**.
10. Любая лаборатория получает статус `RUNTIME VERIFIED` только после полного прохождения на эталонных Ubuntu VM.
11. Инструкция должна содержать ожидаемый результат после критических команд и отдельный раздел диагностики проблем.
12. Студент должен уметь на защите объяснить не только «что получилось», но и **что результат не доказывает**.

---

## 7. Лабораторная среда и известные технические особенности

### Основная среда

- VirtualBox.
- Ubuntu Desktop 24.04.x.
- Ubuntu Server 24.04.x.
- Suricata 8.x.
- На 2026-09-16 в документации курса актуализирована Suricata 8.0.7.

### Offloading / checksum

В виртуальной среде GRO/LRO/GSO/TSO и checksum offloading могут менять представление пакетов в точке захвата.

Для учебных live-запусков Suricata используется:

```text
-k none
```

Это локальное лабораторное решение, а не рекомендация для рабочей среды.

В ЛР №3 есть `prepare-capture.sh`, который best-effort отключает relevant offloads через `ethtool` и показывает фактическое состояние.

### Привязка учебного web-сервиса

В ЛР №3 сервис привязан строго к:

```text
10.13.37.20:8080
```

а не `0.0.0.0:8080`.

Журнал фиксирует:

```text
timestamp
src_ip:src_port
dst_ip:dst_port
method
URI
```

### Артефакты Suricata

Для отдельных прогонов использовать отдельные output directories; нельзя проверять общий `eve.json`, содержащий события предыдущего эксперимента.

---

## 8. Язык курса

Основной язык — **русский**.

Английский термин используется только когда это действительно нужно:

- при первом введении международного термина;
- для общеупотребительной аббревиатуры (`IDS`, `IPS`, `NIDS`, `HIDS`, `WAF` и т. п.);
- в буквальном интерфейсе продукта/стандарта/команды (`http.uri`, `flow:established,to_server`, CLI options и т. д.).

Не использовать без необходимости смешанный стиль:

```text
нежелательно:
detection logic, visibility, evidence, prevention
```

если нормально работают:

```text
логика обнаружения, наблюдаемость, подтверждающие данные, предотвращение
```

При первом употреблении допустимо:

```text
система обнаружения вторжений (Intrusion Detection System, IDS)
```

далее — `IDS` или русский термин.

---

## 9. Визуальный стандарт

Схема должна давать информацию, которую студент быстрее понимает из изображения, чем из абзаца.

Перед добавлением схемы ответить на три вопроса:

1. Что именно студент должен увидеть?
2. Что означает каждая стрелка/линия?
3. Какой вывод следует из схемы?

Если схема — только текст в рамке, она не нужна.

Стрелки должны иметь однозначную семантику. Например:

```text
сплошная линия → основной путь данных
пунктир       → копия данных
отдельная подписанная стрелка → управляющее воздействие
```

Не смешивать в одной неоговорённой стрелке:

- поток данных;
- копию телеметрии;
- причинность;
- управляющую команду;
- логический переход.

Под сложной схемой желательно давать короткий вывод: **«Из схемы видно, что…»**.

### Производственная визуальная стратегия — решение 2026-09-18

Генеративные изображения (Canva/Gemini/image generation) **не использовать как основной production-путь** для технических схем курса. Во время теста на схеме «Один HTTP-запрос — разные точки наблюдения» генераторы многократно вносили семантические ошибки: меняли направление стрелок, смешивали точку наблюдения с источником данных, связывали Linux Audit с сетевым потоком, объединяли систему обнаружения и результат обнаружения, а также искажали русский текст.

Канонический подход:

| Тип | Когда использовать |
|---|---|
| Mermaid | простые flowchart, последовательности, небольшие топологии, логика из 3–8 узлов; предпочтительно, если схема часто меняется вместе с текстом |
| HTML + CSS | карточки, сравнения, семантические блоки, небольшие responsive-модели; это основной путь для визуального языка курса |
| SVG + CSS | ключевые сложные учебные схемы, где важны точная геометрия, семантика стрелок, подписи и визуальная иерархия |
| JavaScript | только meaningful progressive enhancement: переключение представлений, фокус на слоях, раскрытие заранее записанных пояснений |
| PNG/WebP | только реальные скриншоты интерфейсов/CLI/лабораторных артефактов, когда сам внешний вид является доказательством или предметом объяснения |

Правила:

- сначала утверждается **семантическая модель** схемы;
- затем выбирается Mermaid, HTML/CSS или SVG/CSS;
- JavaScript не формирует технические выводы и не является обязательным для чтения материала; без JS весь обязательный контент остаётся доступным;
- новые компоненты используют namespace `idps-` и живут в `idps-system.css` / `idps-system.js`;
- SVG строится детерминированно, а не путём генерации «по картинке»;
- не дублировать одну и ту же мысль одновременно Mermaid и растровой инфографикой без отдельной дидактической причины;
- Canva/Gemini допустимы только как источник визуальных идей, но не как источник технической логики;
- если генеративный вариант требует серии смысловых исправлений, его нужно отбросить, а не продолжать итерации.

Для Главы 3 канонической остаётся **Схема 4 «Один HTTP-запрос — разные точки наблюдения» в Mermaid** из `docs/course/03-detection.md`. Генеративную растровую версию в Главу 3 не добавлять. Если позже потребуется более оформленная версия, перенести уже утверждённую модель в ручной SVG без изменения логики.

Полный визуальный регламент также хранится в `internal/resources/visual-guide.md`.

---

## 10. Ключевые логические ошибки, которые нельзя возвращать

Ниже — не список стилистических предпочтений, а уже найденные дефекты прежней версии курса.

### Не возвращать

```text
Firewall = только L3/L4
IDS = L7
```

Современные продукты могут пересекаться по анализируемым уровням. Различать защитные функции, а не номера OSI.

```text
IDS = passive
IPS = inline
```

Deployment topology и prevention capability — разные оси.

```text
Network / Host / Wireless / Behavioral
```

как четыре равноправные категории. `Network`, `Host` и `Wireless` описывают область/источник наблюдаемых данных, тогда как термин `behavioral` в литературе и продуктах употребляется неоднозначно. Без явного определения контекста его нельзя ставить в один ряд с категориями источника данных.

```text
Behavioral = Anomaly
```

Такое отождествление без определения терминов некорректно. В курсе аномальное обнаружение означает сравнение с моделью, профилем или ожидаемым диапазоном; фиксированное условие над серией событий рассматривается отдельно.

```text
packet → decode → flow → stream → parser → detection
```

как универсальный обязательный pipeline любой IDS. Обнаружение может выполняться на разных представлениях и уровнях обработки.

```text
нет alert → не было traffic
```

или:

```text
нет записи → доказана blind spot
```

Отрицательный результат сам по себе этого не доказывает.

```text
Alert = Incident = Compromise
```

Оповещение — результат работы детектора; степень доказательности зависит от условия и доступных данных.

```text
совпадение простого маркера = False Positive
```

без заранее определённого Detection Objective и объекта оценки.

```text
thresholding исправляет причину FP
```

Threshold управляет частотой/агрегацией оповещений, но не обязан исправлять логическую причину неверного detection condition.

```text
SPAN/TAP = метод обнаружения
```

Это способы предоставить данные сенсору в выбранной точке наблюдения.

```text
application log = полная ground truth сетевой активности
```

Он подтверждает только то, что реально логирует приложение.

```text
одна observation point = видимость всей инфраструктуры
```

Точка оценивается относительно конкретных потоков и задач наблюдения.

---

## 11. Модальности утверждений

Не смешивать четыре разных уровня утверждений:

```text
МОЖЕТ
НАСТРОЕНО
НАБЛЮДАЛОСЬ
ДОКАЗЫВАЕТ
```

Пример:

> Suricata **может** анализировать HTTP URI.

не означает:

> в текущем стенде она **получила** нужный HTTP URI.

А наличие alert не означает:

> приложение было успешно скомпрометировано.

Рекомендуемый стиль интерпретации:

> Наблюдаемый артефакт X поддерживает утверждение Y, но оставляет открытыми предположения Z.

---

## 11.1. Дополнительный банк материалов, подключённый в сентябре 2026

Эти материалы **не меняют структуру курса** и не являются новым каркасом. Они используются только как источник примеров, формулировок, проверочных идей и контекста для уже зафиксированных Глав 1–8.

### Университетский УМКД / syllabus

Файл:

```text
Полный+УМКД_+Коржаспаев+Арман+Ермекович_Системы+обнаружения+и+предотвращения+вторжений_2026.pdf
```

Зафиксированные параметры дисциплины: 4 курс, 7 семестр, 5 ECTS, 30 часов лекций, 15 часов лабораторных, 105 часов СРО, всего 150 часов. Официальный план использовать как академический scope/checklist, а не копировать буквально. Offensive/pentest темы из УМКД при необходимости использовать только как **контролируемые источники событий** для IDPS-экспериментов.

Отдельный рабочий документ:

```text
IDPS_Syllabus_Alignment_v1.md
```

### SANS 2026 — дополнительный материал, не каркас курса

Файл:

```text
c728b2e6-206c-48b7-8ca0-9f0dc5a15a25.pdf
```

Идентифицирован как Joshua Wright, *Dynamic Incident Response: A Framework for Security Teams*, SANS Institute, 2026. Использовать только как **банк материала**. Не перестраивать курс вокруг Incident Response и не переносить operational IR taxonomy как универсальную IDPS-классификацию.

Полезные направления уже отобраны:

- Глава 3: разные источники наблюдения дают взаимодополняющие данные;
- Глава 6: lifecycle правила/детектора — конкретная техника, проверка, tuning, документирование intent/logic;
- Глава 7: alert fatigue, объём данных, limits of visibility, encrypted traffic metadata, evasion;
- Глава 8: проверка результата, ограничение области вывода, triage/verification;
- optional/further study: PCAP → Zeek → RITA и JA4; не добавлять их в обязательную среду до стабилизации текущего runtime.

Не использовать из книги как универсальную таксономию четыре категории `signature / behavioral / ML / hybrid`; это контекст конкретного издания, а не ортогональная классификация источников/методов нашего курса.

---

## 12. Источники и статус утверждений

Рабочий порядок разработки:

```text
литература / стандарты / официальная документация
        ↓
техническая модель
        ↓
объяснение студенту
        ↓
схема
        ↓
лабораторный эксперимент
```

Текущий курс содержит:

```text
docs/resources/sources.md
internal/resources/evidence-register.md
internal/resources/audit-report.md
```

Основные классы источников:

- NIST SP 800-94 — исторический фундаментальный источник по IDPS;
- NIST SP 800-41 — firewall policy;
- Stallings & Brown — учебная фундаментальная база;
- Denning 1987;
- Paxson/Bro;
- Ptacek & Newsham — ambiguity/evasion research;
- Axelsson — base-rate;
- McHugh — critique of IDS evaluation;
- Sommer & Paxson — limits of closed-world ML evaluation;
- официальная Suricata documentation;
- Zeek documentation;
- актуальные RFC;
- CISA/Microsoft/government incident reports для реальных кейсов.

Если техническое утверждение нельзя уверенно связать с источником или воспроизводимым экспериментом, не выдавать его за установленный факт.

---

## 13. Evaluation Contract для будущей Главы 8

При оценке качества detection заранее определить:

1. **Evaluation Unit** — что именно считаем одним объектом оценки.
2. **Detection Objective** — что детектор должен обнаружить.
3. **Ground Truth / Oracle** — откуда известно истинное состояние.
4. **Scoring & Aggregation Rule** — как один или несколько outputs превращаются в TP/FP/TN/FN.
5. **Evaluation Population / Window** — на какой выборке и временном интервале считаются результаты.

Нельзя использовать precision/recall/FPR без ясного объекта оценки.

---

## 14. Реальные и синтетические кейсы

Всегда обозначать тип материала:

- **REAL INCIDENT** — реальный инцидент с источником;
- **REAL DEPLOYMENT** — реальная архитектура/эксплуатация;
- **RESEARCH / CONTROLLED EXPERIMENT** — исследование или лабораторный эксперимент;
- **SYNTHETIC ENGINEERING** — специально созданный учебный сценарий.

Не выдавать synthetic marker/path/PCAP за реальную атаку или incident capture.

---

## 15. Обязательный release gate

Файл `QA_RELEASE_GATE.md` является обязательной процедурой перед утверждением, что аудит достаточен или релиз готов. Он разделяет consistency, дидактику, student-facing UX, static QA, GitHub Pages build QA и runtime laboratory QA.

Если какой-либо применимый gate не выполнялся, это должно быть указано явно; запрещено заменять его более узкой проверкой.

Статус `v2.29-rc2`:

- Gates 1–4: пройдены (`STATIC QA PASSED`);
- Gate 5 GitHub Pages: `QA REQUIRED` — локальная среда не имеет сетевого доступа для установки pinned MkDocs; workflow настроен выполнять build + generated-link check при push;
- Gate 6 runtime: `RUNTIME QA REQUIRED` — выполнить по `TEACHER_CLASSROOM_CHECKLIST.md`.

Повышать candidate до стабильного релиза только после успешного GitHub Pages workflow. Runtime-статус лабораторий ведётся отдельно и не должен скрываться; пользователь согласовал ручную runtime-проверку позже.

## 16. Текущий статус runtime QA

Статическая корректность скриптов и документации не означает runtime verification.

На момент v2.29-rc2:

- ЛР №1 — `DESIGN VERIFIED`, `STATIC QA PASSED`, полный сценарий `RUNTIME QA REQUIRED`;
- ЛР №2 — `DESIGN VERIFIED`, `STATIC QA PASSED`; component-level checks выполнены, полный сценарий `RUNTIME QA REQUIRED`;
- ЛР №3 — `DESIGN VERIFIED`, `STATIC QA PASSED`, полный двухинтерфейсный сценарий `RUNTIME QA REQUIRED`;
- ЛР №4 — `DESIGN VERIFIED`, `STATIC QA PASSED`; shell/Python syntax и analyser simulation выполнены, полный двухмашинный сценарий `RUNTIME QA REQUIRED`;
- подготовка среды — скрипты статически проверены, фактический результат зависит от Ubuntu/VirtualBox и остаётся `RUNTIME QA REQUIRED`.

Для ЛР №4 component check bundle v0.2 использовал детерминированный JSONL из 19 событий и получил четыре ожидаемых результата: `SIGNATURE`, `PROTOCOL`, `THRESHOLD`, `ANOMALY`. Это не заменяет прохождение `setup → preflight → client check → reset → scenario → analysis` на реальных Ubuntu Desktop/Server 24.04.x.

Перед использованием в аудитории необходимо полностью пройти студенческие инструкции на реальном эталонном стенде.

## 17. Текущий маршрут студента

```text
Глава 1
          ↓
00. Подготовка лабораторной среды
          ↓
Практикум 0 — знакомство с рабочей IDPS
          ↓
Глава 2
          ↓
Глава 3
          ↓
ЛР №1 — первое сетевое обнаружение
          ↓
ЛР №2 — один эпизод, два источника
          ↓
Глава 4
          ↓
ЛР №3 — точка наблюдения
          ↓
Глава 5
          ↓
ЛР №4 — четыре основания решения
          ↓
Глава 6 — что такое правила и как они устроены
          ↓
ЛР №5 — СЛЕДУЮЩЕЕ ПРОЕКТИРОВАНИЕ ПОСЛЕ CONSISTENCY TABLE
```

Практикум 0 даёт только операционную карту уже установленной системы и не подменяет Главу 3. Поэтому Глава 3 проходит до ЛР №1–2: только после функциональной модели студент применяет её в первых двух экспериментах.

## 17.1. Содержательная коррекция v2.28-rc5

Перед Главой 6 выполнен целевой аудит согласованности Глав 1–5, самопроверок и ЛР №1–4. Исправлены: порядок Главы 3 и первых лабораторных; терминология Главы 5 и ЛР №4 без ложной «четвёртой behavioral-методологии»; преждевременная корреляция в Главе 2; неоднозначный вопрос Главы 1; остатки student-facing устной защиты; неиспользуемый legacy JavaScript; прямые источники по WAF, checksum offloading и Linux Audit. ЛР №4 обновлена до bundle `v0.2`.

## 17.2. Pre-Chapter 6 consistency patch v2.28-rc5.1

Перед разработкой Главы 6 синхронизированы только внутренние материалы: актуализирован номер release candidate в release-gate; `evidence-register.md` и `audit-report.md` приведены к текущей модели ЛР №4 (`THRESHOLD`, а не отдельная универсальная `behavioral`-методология); формулировка про `behavioral` уточнена как контекстно-зависимый термин, который нельзя использовать как категорию источника данных без явного определения. Student-facing содержание Глав 1–5 и ЛР №1–4 этим patch не изменялось.

## 17.3. Глава 6 — v2.29-rc1

Новая Глава 6 добавлена после успешного Pre-Chapter 6 patch. Она использует знакомое правило ЛР №1 и объясняет rule semantics без преждевременного Detection Engineering: action/header/options, область применения, sticky buffers, flow context, `msg`/`sid`/`rev`, alert/action boundaries и thresholding. В главе пять формативных вопросов. ЛР №5 намеренно не включена в этот релиз.

## 17.4. Визуальная коррекция Главы 3 — v2.29-rc2

В Главу 3 добавлена схема «Один HTTP-запрос — разные точки наблюдения». В ней отдельно показаны логическая точка наблюдения, Suricata в роли NIDS, результат обнаружения (`alert`), лог приложения и Linux Audit. Артефакты сходятся только на этапе интерпретации и проверки; схема не смешивает исходные данные с результатом обнаружения и не устанавливает причинную связь автоматически. Зафиксированы выводы `Alert ≠ Incident` и `нет alert ≠ нет трафика`. Последующие схемы Главы 3 перенумерованы.

## 17.5. Chapter 1 visual/UI reference pass — v2.29-rc4

- полностью мигрированы технические визуальные блоки Главы 1 на reusable `idps-*` components;
- Mermaid оставлен там, где он действительно объясняет topology/flow, а карточки, сравнения и evidence boundaries сделаны HTML/CSS;
- пунктир в Схемах 1–2 больше не используется для значений «запрещено» или «тот же путь»; пунктир зарезервирован за копией наблюдаемых данных;
- switcher теперь корректно деградирует без JavaScript;
- `internal/resources/chapter1-visual-audit.md` фиксирует решения и является шаблоном аудита для Глав 2–6.

## 17.6. Решение по визуалам — handoff 2026-09-18

После серии тестов Canva/Gemini принято решение **не тратить дальнейшее время на генеративные технические инфографики**. Они оказались нестабильны по смыслу и русскому тексту. В production остаются Mermaid, ручной SVG, реальные скриншоты и HTML/CSS-компоненты MkDocs.

Текущая Mermaid-схема 4 в Главе 3 прошла смысловую проверку и является источником истины. Генеративные JPG/PNG, созданные в ходе эксперимента, не являются частью курса и не должны добавляться в `docs/assets/images/`.

## 18. Следующий шаг разработки

### Сначала — consistency table для ЛР №5

До создания ЛР №5 зафиксировать:

```text
понятие из Главы 6
→ действие студента
→ артефакт
→ допустимый вывод
→ что артефакт не доказывает
```

ЛР №5 должна экспериментально закреплять именно **семантику правила**, а не превращаться в справочник Suricata keywords. Наиболее естественный scope: студент читает и минимально изменяет несколько уже понятных правил, проверяет контролируемые положительный и отрицательный случаи и объясняет, какие данные использует каждое условие.

Обязательные ограничения:

- не вводить TP/FP/TN/FN как центральную тему до Главы 7/8;
- не смешивать частоту alert с корректностью detection condition;
- не использовать отсутствие alert как единственное доказательство отсутствия события;
- не переходить к advanced PCRE, fast-pattern tuning, evasion/reassembly или полноценному Detection Engineering lifecycle;
- Suricata остаётся реализацией общего принципа, а не предметом курса.

### Затем

После согласованной ЛР №5 перейти к новой Главе 7 — **«Почему IDS/IPS ошибается и чего она не видит»**.

Runtime-проверка существующих лабораторий остаётся отдельным техническим треком и будет выполнена пользователем вручную с последующей обратной связью.

## 19. Что не делать следующим шагом

Не перепрыгивать сразу к Главе 7 «Почему IDS/IPS ошибается» до проектирования связанной ЛР №5 и закрепления базовой семантики правил.

Не превращать Главу 6 в длинный справочник Suricata keywords или advanced Detection Engineering.

Не смешивать threshold/suppression с качеством самого detection condition.

Не использовать alert count как доказательство качества правила без заранее определённого evaluation objective.

Не вводить новый инструмент только ради разнообразия: сначала использовать уже знакомый Suricata там, где он действительно помогает изучить rule semantics.

Не возвращаться к циклу генерации Canva/Gemini для технических схем. Если Mermaid становится неудобным, следующий шаг — **HTML/CSS либо SVG/CSS по утверждённой семантической модели**, а не новый image prompt.

## 20. Новый чат: рекомендуемый стартовый запрос

После загрузки актуального ZIP можно написать:

> Продолжаем разработку университетского курса «Системы обнаружения и предотвращения вторжений». Это актуальный ZIP проекта. Сначала прочитай `PROJECT_HANDOFF.md`, `mkdocs.yml` и `internal/resources/visual-guide.md`. Структуру Глав 1–8 не менять. В `v2.29-rc3` начат отдельный визуальный рефакторинг с design system `idps-system.css` + `idps-system.js`; в `v2.29-rc4` Глава 1 завершена как reference implementation, существующие `extra.css` и `course.js` пока сохраняются для совместимости. Активный UI-трек: перейти к Главе 2, затем последовательно пройти Главы 3–6, используя Mermaid + HTML/CSS + SVG/CSS и минимальный progressive-enhancement JS; Canva/Gemini для технических схем не использовать. Схема 4 Главы 3 уже утверждена и остаётся Mermaid. После visual/UI pass следующий содержательный шаг остаётся прежним: consistency table и проектирование ЛР №5 по Главе 6. Runtime-проверка лабораторий идёт отдельным ручным треком пользователя.

## 21. Правило обновления этого файла

После каждого крупного релиза обновлять минимум:

- текущую стабильную версию;
- готовые главы;
- готовые лабораторные и версии bundles;
- runtime QA status;
- новые найденные логические дефекты;
- следующий шаг.

Если `PROJECT_HANDOFF.md` расходится с текущим кодом/страницами, источником истины считаются **актуальные файлы проекта**, а handoff должен быть исправлен в том же релизе.
